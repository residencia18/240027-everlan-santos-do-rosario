import { PiscaDirective } from './pisca.directive';

describe('PiscaDirective', () => {
  it('should create an instance', () => {
    const directive = new PiscaDirective();
    expect(directive).toBeTruthy();
  });
});
