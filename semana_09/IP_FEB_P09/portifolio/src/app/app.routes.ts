import { Routes } from '@angular/router';
import { WikiAPIComponent } from './portifolios/wiki-api/wiki-api.component';

export const routes: Routes = [
    {
        path: 'wikiapi', component: WikiAPIComponent
    }
];
