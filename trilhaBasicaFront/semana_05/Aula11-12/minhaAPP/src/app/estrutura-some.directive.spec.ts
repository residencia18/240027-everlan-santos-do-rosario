import { EstruturaSomeDirective } from './estrutura-some.directive';

describe('EstruturaSomeDirective', () => {
  it('should create an instance', () => {
    const directive = new EstruturaSomeDirective();
    expect(directive).toBeTruthy();
  });
});
