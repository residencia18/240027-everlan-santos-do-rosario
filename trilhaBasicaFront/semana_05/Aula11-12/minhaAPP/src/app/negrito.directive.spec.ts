import { NegritoDirective } from './negrito.directive';

describe('NegritoDirective', () => {
  it('should create an instance', () => {
    const directive = new NegritoDirective();
    expect(directive).toBeTruthy();
  });
});
